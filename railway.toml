[build]
builder = "NIXPACKS"
buildCommand = "bun install && bun x prisma generate && bun x prisma db push"

[deploy]
startCommand = "bun x next start -p $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[environments.production.deploy]
startCommand = "bun x next start -p $PORT"
